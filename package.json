{
  "name": "nexus-ai-coding",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend": "cd backend && npm install && (npm run build || echo 'no build')"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jszip": "^3.10.1",
    "tree-kill": "^1.2.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "eslint": "^9.39.2"
  }
}
