1. Add universal scrollbar selector to frontend/src/app/globals.css
2. Run deployment command: npx convex deploy --cmd "npm run build" --functions ./convex

All other configuration files (convex.json, projects.ts, tsconfig.json) are already correctly configured per the deployment protocol.