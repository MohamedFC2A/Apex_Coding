# Copy this file to `.env` (it is ignored by git), then edit values.

# Bearer token required by the preview runner API.
PREVIEW_RUNNER_TOKEN=replace-me

# Used only to generate the preview URLs returned by the API.
# For local dev on Windows, keep the default path-based sessions and use localhost.
PREVIEW_DOMAIN=localhost
PREVIEW_PROTOCOL=http
PREVIEW_PUBLIC_PORT=8080
PREVIEW_URL_MODE=path

