{
  "name": "nexus-ai-coding",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm --workspace frontend run dev\" \"node api/server.js\"",
    "build": "npm run build:frontend && node -e \"const fs=require('fs');const path=require('path');const src=path.join('frontend','.next');const dst=path.join('.next');fs.rmSync(dst,{recursive:true,force:true});fs.cpSync(src,dst,{recursive:true});\" && npm run build:api",
    "build:frontend": "npm --workspace frontend run build",
    "build:api": "node --check api/index.js"
  },
  "dependencies": {
    "convex": "^1.31.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jszip": "^3.10.1",
    "tree-kill": "^1.2.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "eslint": "^9.39.2"
  }
}
