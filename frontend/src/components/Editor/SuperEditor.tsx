'use clent';

iSEffctforwardRef, IperativeHandlePuCod2 XChevronFoldFoderOpenFilBranZp,  Sparkles
setngsupdaSttgysettings.y(:any) y fontSize:settings. wordWrap:settings.settings. lineNumbers:settings.falssettings.settings.settings.settings.ysettings.: settings.fontSize: settings.wordWrap: settings.lineNumbersvslightLighth-lacHghCs>
          <div className="absolute top-16 right-4 bg-gray-900 border border-gray-700 rounded-lg shadow-2xl z-50 w-80">
            <div className="p-2"
              <input
                type="text"
                placeholder="Type a command..."           className="w-fullpx-3py-2bg-gray-800text-whiterounded border border-gray-600 focus:border-cyan-500 focus:outline-none"
                autoFocus
              />
              <div className="mt-2 max-h-64 overflow-y-auto">
                <button
                  onClick={() => {
                    saveCurrentFile();
                    setShowCommandPalette(false);
                  }}
                  className="flex items-center w-full px-3 py-2 text-left text-gray-300 hover:bg-gray-800 rounded transition-colors"
                >
                  <Save className="w-4 h-4 mr-3 text-cyan-400" />
                  <span>Save File</span>
                </button>
                <button
                  onClick={() => {
                    formatCode();
                    setShowCommandPalette(false);
                  }}
                  className="flex items-center w-full px-3 py-2 text-left text-gray-300 hover:bg-gray-800 rounded transition-colors"
                >
                  <Code2 className="w-4 h-4 mr-3 text-cyan-400" />
                  <span>Format Code</span>
                </button>
                <button
                  onClick={() => 
                    analyzeCurrentde();
                    setShowCo(false);
                  }
                  className="flex items-center w-full px-3 py-2 text-left text-gray-300 hover:bg-gray-800 rounded transition-colors"
                >
                  <Brain className="w-4 h-4 mr-3 text-cyan-400" />
                  <span>AI Analysis</span>
                </button>
              </div>
            </div>
          </div>